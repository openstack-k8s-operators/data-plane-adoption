registry_name: "quay.io"
registry_namespace: "podified-antelope-centos9"
image_tag: "current-podified"
ansibleSSHPrivateKeySecret: dataplane-adoption-secret
deployStrategy:
  deploy: false
edpm_encoded_privatekey: "{{ edpm_privatekey|b64encode }}"
