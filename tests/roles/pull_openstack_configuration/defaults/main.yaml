os_diff_etc_dir: /etc/os-diff
os_diff_dir: tmp/os-diff
os_diff_data_dir: tmp/os-diff
source_os_diff_config_ip: "{% if ipv6_enabled | default(false) %}{{ ctlplane_prefix_ipv6 | default('2620:cf:cf:aaaa') }}::76{% else %}{{ ctlplane_prefix | default('192.168.122') }}.100{% endif %}"
